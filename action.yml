name: 'my first container action in Github Action'
description: 'Container Action'
author: 'Hao Sky Zhou'
inputs:
  myInput:
    description: 'greeting to use'
    required: true
    default: 'Awesome Guru'
outputs:
  myOutput:
    description: 'total memory'
runs:
  using: 'docker'
  image: 'Dockerfile'
